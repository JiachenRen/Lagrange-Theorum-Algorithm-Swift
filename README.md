# Lagrange-Theorum-Algorithm-Swift
An algorithm that decomposes a natural number into the sum of 4 squared numbers written in swift. Note: swift's dictionary is used for hash-mapping like acceleration.
